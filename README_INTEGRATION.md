# Калькулятор себестоимости (Prospekt.Calc)

Полнофункциональный калькулятор себестоимости полиграфической продукции для интеграции с 1С-Битрикс.

## 🎯 Назначение

Приложение предназначено для администраторов интернет-магазина на 1С-Битрикс. Работает **ТОЛЬКО внутри iframe** админ-панели Битрикс.

### Ключевые особенности

- ✅ **Черновиковый режим**: Все изменения локальны до нажатия «Сохранить»
- ✅ **Два режима работы**: NEW_CONFIG (создание) и EXISTING_CONFIG (редактирование)
- ✅ **PostMessage API**: Полная интеграция с Битрикс через стандартный протокол
- ✅ **Независимость от ID**: Работа с любыми ID инфоблоков и товаров
- ✅ **Стабильные pwcode**: Все элементы UI имеют уникальные идентификаторы

## 📦 Установка и запуск

```bash
# Установка зависимостей
npm install

# Запуск в режиме разработки
npm run dev

# Сборка для продакшена
npm run build
```

После сборки файлы размещаются на сервере по пути:
```
/local/apps/prospektweb.calc/
```

## 📖 Документация

### Основная документация

- **[BITRIX_INTEGRATION_CHANGELOG.md](./BITRIX_INTEGRATION_CHANGELOG.md)** - История изменений интеграции с Bitrix
- **[IMPLEMENTATION_SUMMARY.md](./IMPLEMENTATION_SUMMARY.md)** - Детали реализации режима Bitrix Deploy
- **[BITRIX_DEPLOY.md](./BITRIX_DEPLOY.md)** - Инструкция по развертыванию
- **[README.md](./README.md)** - Основное руководство

### Дополнительные материалы

- **[PRD.md](./PRD.md)** - Product Requirements Document
- **[BITRIX_INTEGRATION_CHANGELOG.md](./BITRIX_INTEGRATION_CHANGELOG.md)** - История изменений интеграции с Bitrix
- **[IMPLEMENTATION_SUMMARY.md](./IMPLEMENTATION_SUMMARY.md)** - Детали реализации
- **[test-integration.html](./test-integration.html)** - Тестовая страница для локальной отладки

## 🔌 Быстрый старт интеграции

### 1. Встраивание в Битрикс

```php
<?php
// В админ-панели Битрикс создайте страницу с iframe
?>
<!DOCTYPE html>
<html>
<body>
    <iframe 
        id="calc-iframe" 
        src="/local/apps/prospektweb.calc/index.html"
        style="width: 100vw; height: 100vh; border: none;"
    ></iframe>
    
    <script src="/local/js/calculator-integration.js"></script>
</body>
</html>
```

### 2. Базовая интеграция (JavaScript)

```javascript
// calculator-integration.js

const iframe = document.getElementById('calc-iframe');

// Прослушивание сообщений от калькулятора
window.addEventListener('message', (event) => {
    const msg = event.data;
    
    // Проверка формата
    if (!msg || msg.target !== 'bitrix') return;
    if (msg.source !== 'prospektweb.calc') return;
    
    console.log('[Bitrix] Received:', msg.type);
    
    switch (msg.type) {
        case 'READY':
            // Калькулятор готов, отправляем INIT
            sendInit();
            break;
            
        case 'CALC_RUN':
            // Запуск расчёта
            handleCalculation(msg.payload);
            break;
            
        case 'CLOSE_REQUEST':
            // Закрытие окна (содержит информацию о сохранении)
            handleClose(msg.payload);
            break;
    }
});

function sendInit() {
    iframe.contentWindow.postMessage({
        source: 'bitrix',
        target: 'prospektweb.calc',
        type: 'INIT',
        payload: {
            mode: 'NEW_CONFIG',  // или 'EXISTING_CONFIG'
            context: {
                siteId: 's1',
                userId: '123',
                lang: 'ru',
                timestamp: Date.now()
            },
            iblocks: {
                materials: 10,
                operations: 11,
                equipment: 12,
                details: 13,
                calculators: 14,
                configurations: 15
            },
            selectedOffers: [
                { id: 525, productId: 100, name: 'Постер A3 - 100 шт' },
                { id: 526, productId: 100, name: 'Постер A3 - 500 шт' }
            ]
        },
        timestamp: Date.now()
    }, '*');
}

async function handleCalculation(payload) {
    // Обработка запроса на расчёт через API Битрикс
    const response = await fetch('/local/ajax/calc_run.php', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(payload)
    });
    
    const result = await response.json();
    
    // Отправка результата обратно
    iframe.contentWindow.postMessage({
        source: 'bitrix',
        target: 'prospektweb.calc',
        type: 'CALC_INFO',
        payload: result,
        timestamp: Date.now()
    }, '*');
}
```

### 3. Важные моменты

⚠️ **До нажатия «Сохранить» ничего не записывается в Битрикс**  
Все изменения — локальный черновик.

⚠️ **ID инфоблоков зависят от сайта**  
Передавайте реальные ID через INIT, не хардкодите.

⚠️ **Используйте pwcode для идентификации элементов**  
Все кнопки и элементы имеют атрибут `pwcode` для стабильной идентификации.

## 🔧 Протокол postMessage

### Основные сообщения

| Направление | Тип | Назначение |
|-------------|-----|------------|
| iframe → Битрикс | `READY` | Калькулятор загружен и готов |
| Битрикс → iframe | `INIT` | Стартовые данные и конфигурация |
| iframe → Битрикс | `INIT_DONE` | Инициализация завершена |
| iframe → Битрикс | `CALC_RUN` | Запуск расчёта |
| Битрикс → iframe | `CALC_INFO` | Информация о расчёте |
| Битрикс → iframe | `RESPONSE` | Единый ответ на любой REQUEST |
| Обе стороны | `ERROR` | Ошибка в процессе работы |
| iframe → Битрикс | `CLOSE_REQUEST` | Запрос на закрытие окна |

Подробности см. в [BITRIX_INTEGRATION_CHANGELOG.md](./BITRIX_INTEGRATION_CHANGELOG.md) и [IMPLEMENTATION_SUMMARY.md](./IMPLEMENTATION_SUMMARY.md)

## 🎨 Технологии

- **React 19** - UI фреймворк
- **TypeScript** - Типизация
- **Tailwind CSS** - Стилизация
- **shadcn/ui** - Компоненты
- **Vite** - Сборщик
- **Phosphor Icons** - Иконки
- **Sonner** - Уведомления

## 🧪 Тестирование

### Локальная отладка

1. Запустите dev-сервер:
```bash
npm run dev
```

2. Откройте `test-integration.html` в браузере

3. Используйте панель управления для тестирования различных сценариев

### Тестирование с Битрикс

1. Соберите проект:
```bash
npm run build
```

2. Разместите файлы на сервере в `/local/apps/prospektweb.calc/`

3. Создайте тестовую страницу в админке Битрикс с iframe

4. Настройте обработчики postMessage согласно документации

## 📁 Структура проекта

```
src/
├── components/
│   ├── calculator/        # Компоненты калькулятора
│   │   ├── DetailCard.tsx
│   │   ├── BindingCard.tsx
│   │   ├── HeaderSection.tsx
│   │   ├── CostPanel.tsx
│   │   ├── PricePanel.tsx
│   │   └── ...
│   └── ui/               # UI компоненты (shadcn)
├── hooks/
│   ├── use-mobile.ts
│   ├── use-postmessage.ts  # Hook для postMessage API
│   └── use-custom-drag.ts
├── lib/
│   ├── postmessage-bridge.ts  # Ядро postMessage API
│   ├── types.ts          # TypeScript типы
│   ├── mock-data.ts      # Тестовые данные
│   └── utils.ts
├── types/
│   └── global.d.ts       # Глобальные декларации типов
├── App.tsx               # Главный компонент
└── index.css             # Стили
```

## 🔍 Справочник pwcode

Основные элементы UI:

### Шапка
- `header` - Контейнер шапки
- `btn-menu` - Кнопка меню
- `btn-refresh` - Обновление данных
- `tab-materials`, `tab-operations`, `tab-equipment`, `tab-details` - Вкладки

### Основная область
- `mainarea` - Контейнер рабочей области
- `detail-card` - Карточка детали
- `binding-card` - Карточка скрепления
- `btn-create-binding` - Создать скрепление

### Футер
- `footer` - Контейнер футера
- `btn-gabves` - Габариты/Вес
- `btn-cost` - Себестоимость
- `btn-price` - Отпускные цены
- `btn-test-calc` - Тестовый расчёт
- `btn-full-calc` - Полный расчёт
- `btn-save` - Сохранить
- `btn-close` - Закрыть

Полный список см. в [docs/bitrix-integration.md](./docs/bitrix-integration.md)

## 🤝 Поддержка

Для вопросов по интеграции:
1. Изучите [docs/bitrix-integration.md](./docs/bitrix-integration.md)
2. Посмотрите примеры в разделе документации
3. Используйте `test-integration.html` для отладки

## 📝 Версия протокола

**Текущая версия:** 1.0.0  
**Код протокола:** pwrt-v1

При расширении протокола следуйте правилам Semantic Versioning, описанным в документации.

## 📄 Лицензия

MIT License - Copyright GitHub, Inc.

